import React from 'react'
import {useSelector} from "react-redux"
import SectionTitle from "./SectionTitle"
const CheckoutForm = () => {
    return(
        <>
        <h2 className="text-3xl">CheckoutForm</h2>
    </>
  )
}

export default CheckoutForm